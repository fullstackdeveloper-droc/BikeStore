/* import { TestBed } from '@angular/core/testing';

import { PaginationPonyService } from '../pagination/pagination-pony.service';

describe('PaginationPonyService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: PaginationPonyService = TestBed.get(PaginationPonyService);
    expect(service).toBeTruthy();
  });
});
 */